# vmlinux.h is a 3.5 MB dump of a kernel's BTF (bpftool btf dump ... format c),
# regenerated by CI on every push. GitHub counted it as hand-written C and
# labelled this Go repository "C" (3.53 MB of C against 0.86 MB of Go on
# 2026-09-09). The one C file a person wrote is bpf/connect.c beside it.
internal/ebpfcapture/bpf/vmlinux.h linguist-generated=true
